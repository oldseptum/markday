{
  "name": "markday",
  "version": "0.28.0",
  "description": "Calendar and task manager inside your Markdown daily notes (Obsidian plugin).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
